diff --git a/frigate/api/app.py b/frigate/api/app.py
index d954a74..13a8b02 100644
--- a/frigate/api/app.py
+++ b/frigate/api/app.py
@@ -492 +492 @@ def nvinfo():
-    dependencies=[Depends(allow_any_authenticated())],
+    dependencies=[Depends(require_role(["admin"]))],
diff --git a/frigate/api/camera.py b/frigate/api/camera.py
index 1dae5ae..204a77a 100644
--- a/frigate/api/camera.py
+++ b/frigate/api/camera.py
@@ -12 +12 @@ import requests
-from fastapi import APIRouter, Depends, Query, Request, Response
+from fastapi import APIRouter, Body, Depends, Query, Request, Response
@@ -196,2 +196,6 @@ def go2rtc_delete_stream(stream_name: str):
-@router.get("/ffprobe", dependencies=[Depends(require_role(["admin"]))])
-def ffprobe(request: Request, paths: str = "", detailed: bool = False):
+@router.post("/ffprobe", dependencies=[Depends(require_role(["admin"]))])
+def ffprobe(
+    request: Request,
+    paths: str = Body(default=""),
+    detailed: bool = Body(default=False),
+):
@@ -328,2 +332,6 @@ def ffprobe(request: Request, paths: str = "", detailed: bool = False):
-@router.get("/ffprobe/snapshot", dependencies=[Depends(require_role(["admin"]))])
-def ffprobe_snapshot(request: Request, url: str = "", timeout: int = 10):
+@router.post("/ffprobe/snapshot", dependencies=[Depends(require_role(["admin"]))])
+def ffprobe_snapshot(
+    request: Request,
+    url: str = Body(default=""),
+    timeout: int = Body(default=10, ge=1, le=15),
+):
diff --git a/web/src/components/overlay/CameraInfoDialog.tsx b/web/src/components/overlay/CameraInfoDialog.tsx
index a15d9c5..39d445f 100644
--- a/web/src/components/overlay/CameraInfoDialog.tsx
+++ b/web/src/components/overlay/CameraInfoDialog.tsx
@@ -36,4 +36,2 @@ export default function CameraInfoDialog({
-      .get("ffprobe", {
-        params: {
-          paths: `camera:${camera.name}`,
-        },
+      .post("ffprobe", {
+        paths: `camera:${camera.name}`,
diff --git a/web/src/components/settings/wizard/Step2ProbeOrSnapshot.tsx b/web/src/components/settings/wizard/Step2ProbeOrSnapshot.tsx
index 7a52c8d..c2534d3 100644
--- a/web/src/components/settings/wizard/Step2ProbeOrSnapshot.tsx
+++ b/web/src/components/settings/wizard/Step2ProbeOrSnapshot.tsx
@@ -62,2 +62 @@ export default function Step2ProbeOrSnapshot({
-        const probeResponse = await axios.get("ffprobe", {
-          params: { paths: uri, detailed: true },
+        const probeResponse = await axios.post("ffprobe", { paths: uri, detailed: true }, {
@@ -134,2 +133 @@ export default function Step2ProbeOrSnapshot({
-            const snapshotResponse = await axios.get("ffprobe/snapshot", {
-              params: { url: uri },
+            const snapshotResponse = await axios.post("ffprobe/snapshot", { url: uri }, {
diff --git a/web/src/components/settings/wizard/Step3StreamConfig.tsx b/web/src/components/settings/wizard/Step3StreamConfig.tsx
index 380f0a7..a66bd80 100644
--- a/web/src/components/settings/wizard/Step3StreamConfig.tsx
+++ b/web/src/components/settings/wizard/Step3StreamConfig.tsx
@@ -188,2 +188 @@ export default function Step3StreamConfig({
-        const response = await axios.get("ffprobe", {
-          params: { paths: stream.url, detailed: true },
+        const response = await axios.post("ffprobe", { paths: stream.url, detailed: true }, {
diff --git a/web/src/components/settings/wizard/Step4Validation.tsx b/web/src/components/settings/wizard/Step4Validation.tsx
index 8352a1c..e0a1431 100644
--- a/web/src/components/settings/wizard/Step4Validation.tsx
+++ b/web/src/components/settings/wizard/Step4Validation.tsx
@@ -72,2 +72 @@ export default function Step4Validation({
-        const response = await axios.get("ffprobe", {
-          params: { paths: stream.url, detailed: true },
+        const response = await axios.post("ffprobe", { paths: stream.url, detailed: true }, {
